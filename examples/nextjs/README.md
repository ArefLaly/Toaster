# Toastra Next.js example

App Router demo with a server page and a client island.

```bash
cd examples/nextjs
npm install
npm run dev
```

`toast()` is only called from `client-demo.tsx`. `<Toastra />` lives in `providers.tsx`.
